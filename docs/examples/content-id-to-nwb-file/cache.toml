[cache]
name = "content-id-to-nwb-file"

# A pure filter over the upstream file, so the records are copied through as-is rather than merged
# into a lookup: `records` is the shape that preserves that.
[[inputs]]
name = "content-id-to-usage-dandiset-path"
format = "records"

[description]
authors = ["Cody Baker"]
